[build]
  base    = "frontend"
  command = "npm run build:prod"
  publish = "dist"

# SPA fallback — all routes serve index.html so Vue Router works
[[redirects]]
  from   = "/*"
  to     = "/index.html"
  status = 200
